{
  "scripts": {
    "dev": "vite",
    "build": "shx rm -r docs && vite build --mode=production",
    "serve": "vite preview"
  },
  "devDependencies": {
    "browser-sync": "^2.26.14",
    "shx": "^0.3.3",
    "vite": "^2.2.1"
  }
}
